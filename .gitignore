/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# --- NCAT: credentials & local scratch (NEVER commit) ---
docs.txt
*.session.sql

# --- NCAT: bulky raw brand/source masters ---
# Web-optimised copies used by the app live in /public/brand (tracked).
# The multi-MB masters stay out of the repo — keep them in shared storage.
/NCAT_Brand_Assets/
/aircrafts_svgs/
/Aircrafts/
/NCAT Logo.png

# --- NCAT: session tooling & empty placeholders ---
/skill-observations/
/office.ncatfmd.com.ng/


// The NCAT AppLayout is the single authenticated shell. Kept as an alias so
// Breeze's Profile pages (which import AuthenticatedLayout with a `header`
// prop) render inside it unchanged.
export { default } from '@/Layouts/AppLayout';
